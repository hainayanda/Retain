use_frameworks!

platform :ios, '13.0'

target 'Retain_Example' do
  pod 'Retain', :path => '../'

  target 'Retain_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 7.0.0'
    pod 'Nimble', '~> 12.0.0'
  end
end
